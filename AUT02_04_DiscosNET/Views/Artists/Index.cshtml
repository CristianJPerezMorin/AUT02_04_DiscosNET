@model IEnumerable<AUT02_04_DiscosNET.Models.Artist>

@{
    ViewData["Title"] = "Artistas";
}

<h1>Listado de Artistas</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ArtistId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.ArtistId">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.ArtistId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
